{
  "name": "@streampay/database",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc && pnpm generate",
    "dev": "tsc -w",
    "generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0"
  },
  "devDependencies": {
    "@streampay/shared-config": "workspace:*",
    "prisma": "^5.14.0",
    "typescript": "^5.4.5"
  }
}
